A small call logging system to log incoming and outgoing calls.

Starts logging your call with a single click on a button.
Allows to add company's name, person's name, but also can log call transfers, everything is timestamped.

Easy view of your call, where you can sort the columns to display what you need.
